# =============================================================================
# DESIGN-TO-CODE SYSTEM - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit the .env file - it contains sensitive API keys
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: API Keys (must be set)
# -----------------------------------------------------------------------------

# OpenAI API Key (get from: https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Figma Access Token (get from: https://www.figma.com/settings - Personal Access Tokens)
FIGMA_ACCESS_TOKEN=figd_your-figma-token-here

# -----------------------------------------------------------------------------
# OPTIONAL: Model Configuration
# -----------------------------------------------------------------------------

# Default AI model to use (default: gpt-4o)
# Options: gpt-4o, gpt-4o-mini, o3-mini
DEFAULT_MODEL=gpt-4o

# Fallback model if default fails (default: gpt-4o-mini)
FALLBACK_MODEL=gpt-4o-mini

# Available models as comma-separated list
AVAILABLE_MODELS=gpt-4o,gpt-4o-mini,o3-mini

# -----------------------------------------------------------------------------
# OPTIONAL: Model Selection Thresholds
# -----------------------------------------------------------------------------

# Complexity threshold for simple tasks (1-10, default: 3)
# Tasks with complexity <= this value use faster/cheaper models
SIMPLE_THRESHOLD=3

# Complexity threshold for complex tasks (1-10, default: 7)
# Tasks with complexity >= this value use more powerful models
COMPLEX_THRESHOLD=7

# Prefer speed over quality (true/false, default: false)
PREFER_SPEED=false

# Prefer quality over speed (true/false, default: false)
PREFER_QUALITY=false

# -----------------------------------------------------------------------------
# OPTIONAL: Cost Management
# -----------------------------------------------------------------------------

# Maximum cost per session in USD (default: 5.00)
MAX_SESSION_COST=5.00

# Maximum cost per task in USD (default: 0.50)
MAX_TASK_COST=0.50

# Enable cost tracking (true/false, default: true)
ENABLE_COST_TRACKING=true

# Enable cost optimization (true/false, default: true)
ENABLE_COST_OPTIMIZATION=true

# -----------------------------------------------------------------------------
# OPTIONAL: Figma Configuration
# -----------------------------------------------------------------------------

# Default Figma file ID (optional - can be provided via CLI)
# FIGMA_FILE_ID=your-file-id

# Figma URL (optional - can be provided via CLI)
# FIGMA_URL=https://figma.com/file/...

# -----------------------------------------------------------------------------
# OPTIONAL: Output Configuration
# -----------------------------------------------------------------------------

# Output path for generated components (default: nextjs-app/ui)
OUTPUT_PATH=nextjs-app/ui

# -----------------------------------------------------------------------------
# OPTIONAL: Development & Debugging
# -----------------------------------------------------------------------------

# Enable debug mode (true/false, default: false)
DEBUG=false

# Log level: error, warn, info, debug (default: info)
LOG_LEVEL=info

# Enable performance monitoring (true/false, default: true)
ENABLE_PERFORMANCE_MONITORING=true

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Get your OpenAI API key:
#    - Visit: https://platform.openai.com/api-keys
#    - Create new secret key
#    - Copy and paste into OPENAI_API_KEY above
#
# 3. Get your Figma access token:
#    - Visit: https://www.figma.com/settings
#    - Scroll to "Personal Access Tokens"
#    - Generate new token
#    - Copy and paste into FIGMA_ACCESS_TOKEN above
#
# 4. (Optional) Adjust other settings based on your needs
#
# 5. Save the file and run the workflow!
#
# =============================================================================
